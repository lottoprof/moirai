# Node / pnpm
node_modules/
.pnpm-store/

# Astro / Vite
dist/
.astro/

# Wrangler / Cloudflare
.wrangler/
# worker-configuration.d.ts — авто-генерится `wrangler types` после
# pnpm install (см. package.json scripts.prepare). Не коммитим:
# меняется при каждой правке wrangler.toml + 500KB шум.
worker-configuration.d.ts

# Secrets — wrangler secret put / .dev.vars only
.env
.env.*
.dev.vars
.secrets
secrets/
*.pem
*.key

# TypeScript / build artefacts
*.tsbuildinfo
coverage/

# Editor / IDE (per-user, не шарим)
.vscode/
.idea/

# Editor / OS
.DS_Store
*.log

# Старый worktree от factory — удалён; запрет на повторное появление
.agent/worktrees/
.playwright-mcp/
*.png
